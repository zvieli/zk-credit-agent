[package]
name = "combined"
type = "bin"
authors = [""]

[dependencies]
common = { path = "../common" }
keccak256 = { tag = "v0.1.2", git = "https://github.com/noir-lang/keccak256" }

[profile.release]
optimize = true
fold_unconstrained = true